# ساخت اپ فقط با گوشی (بدون کامپیوتر)

این پوشه یه پروژه کامل اندرویده. چون کامپیوتر نداری، به‌جای Android Studio،
از **GitHub** (رایگان) استفاده می‌کنیم که خودش تو فضای ابری اپ رو می‌سازه و
فایل نصبی (APK) رو تحویلت می‌ده — تو فقط با مرورگر گوشیت کار می‌کنی.

## چیزهایی که لازم داری
- یه گوشی اندروید با اینترنت (همینی که داری)
- برنامه **Termux** (رایگان) — نصبش رو از F-Droid انجام بده، نه Play Store
  (نسخه Play Store قدیمیه و درست کار نمی‌کنه): https://f-droid.org
- یه حساب رایگان GitHub: https://github.com/signup

## مرحله ۱: نصب Termux و Git
1. F-Droid رو نصب کن، بعد از توش Termux رو نصب کن
2. Termux رو باز کن و این دستورها رو بزن:
   ```
   pkg update -y
   pkg install git -y
   ```

## مرحله ۲: ساخت ریپازیتوری در GitHub
1. با مرورگر گوشیت برو به github.com و ثبت‌نام کن (اگه نداری)
2. دکمه "New repository" بزن
3. اسمش رو بذار مثلاً `crypto-bot`
4. حتماً گزینه **Public** یا **Private** رو انتخاب کن (هر دو کار می‌کنه) و Create Repository بزن

## مرحله ۳: آپلود فایل‌های پروژه
راحت‌ترین راه برای گوشی: توی صفحه ریپازیتوری‌ت، دکمه **"Add file" → "Upload files"** رو بزن،
و فایل zip این پروژه رو که برات ساختم انتخاب کن و آپلودش کن، بعد **Commit changes** رو بزن.

اگه گیت‌هاب zip رو خودش extract نکرد (معمولاً نمی‌کنه)، به‌جاش از روش Termux استفاده کن:
```
cd ~
# فایل zip رو دانلود کن (لینکی که از گوشیت باز کردی رو اینجا بذار) یا با gh/curl بگیرش
unzip crypto-bot-project.zip
cd project
git init
git remote add origin https://github.com/USERNAME/crypto-bot.git
git add .
git commit -m "اولین نسخه ربات"
git branch -M main
git push -u origin main
```
(به‌جای USERNAME اسم کاربری گیت‌هابت رو بذار؛ موقع push ممکنه بخواد یوزرنیم/پسورد یا Personal Access Token بزنی — گیت‌هاب توضیح می‌ده چطور بسازیش)

## مرحله ۴: صبر کن تا GitHub خودش اپ رو بسازه
1. توی صفحه ریپازیتوری‌ت، برو به تب **Actions**
2. یه اجرا (workflow run) به اسم "Build APK" می‌بینی که داره اجرا می‌شه (چند دقیقه طول می‌کشه)
3. وقتی تیک سبز خورد، روی همون اجرا کلیک کن
4. پایین صفحه، بخش **Artifacts** رو پیدا کن و فایل `app-debug` رو دانلود کن (یه zip حاوی فایل APK)

## مرحله ۵: نصب روی گوشی
1. فایل zip دانلودی رو extract کن (با یه اپ File Manager معمولی)
2. فایل `app-debug.apk` رو پیدا کن و روش بزن تا نصب بشه
3. اگه گوشی اجازه نداد، توی تنظیمات باید "نصب از منابع ناشناس" (Install unknown apps) رو برای مرورگر/فایل منیجرت فعال کنی — گوشی خودش راهنماییت می‌کنه

## اگه به مشکل خوردی
هر ارور یا پیام قرمزی که توی Actions یا موقع نصب دیدی، عکسشو بگیر یا متنشو کپی کن و برام بفرست،
همون‌جا با هم حلش می‌کنیم.
